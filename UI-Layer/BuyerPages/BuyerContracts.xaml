<Page x:Class="TMS.BuyerContracts"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TMS"
      mc:Ignorable="d" 
      d:DesignHeight="420" d:DesignWidth="650"
      Title="BuyerContracts"
      Background="White">

    <Grid>
        <DataGrid x:Name="ContractData" Margin="60,55,60,80">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Contract ID" Binding="{Binding Path=ContractId}" Width="*"/>
                <DataGridTextColumn Header="Customer" Binding="{Binding Path=CName}" Width="*"/>
                <DataGridTextColumn Header="Job Type" Binding="{Binding Path=JType}" Width="*"/>
                <DataGridTextColumn Header="Van" Binding="{Binding Path=VType}" Width="*"/>
                <DataGridTextColumn Header="Origin" Binding="{Binding Path=CName}" Width="*"/>
                <DataGridTextColumn Header="Destination" Binding="{Binding Path=Dest}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Path=Quant}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="RequestOrder" Content="Request Order" Height="30" Width="100" Margin="490,345,60,45" Click="RequestOrder_Click"/>
    </Grid>
</Page>
